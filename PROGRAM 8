<!DOCTYPE html>
<html>
<head>
  <title>Best Drink Deal</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      padding: 40px;
      background-color: #f4f4f4;
      text-align: center;
    }

    h2 {
      color: #333;
    }

    button {
      padding: 10px 20px;
      font-size: 16px;
      background-color: #008cba;
      color: white;
      border: none;
      border-radius: 5px;
      cursor: pointer;
    }

    button:hover {
      background-color: #005f73;
    }

    .result {
      margin-top: 20px;
      font-size: 18px;
      font-weight: bold;
      color: green;
    }
  </style>
</head>
<body>

  <h2>Which Drink is a Better Deal?</h2>

  <p>Drink A: Rs.25 for size 11</p>
  <p>Drink B: Rs.23 for size 9</p>

  <button onclick="deal()">Check Deal</button>

  <div class="result" id="output"></div>

  <script>
    function deal() {
      // Drink A
      let costA = 25;
      let sizeA = 11;
      let costPerUnitA = costA / sizeA;

      // Drink B
      let costB = 23;
      let sizeB = 9;
      let costPerUnitB = costB / sizeB;

      let output = document.getElementById("output");

      // Compare
      if (costPerUnitA < costPerUnitB) {
        output.innerHTML = "Drink A is a better deal. Choose Drink A!";
      } else if (costPerUnitB < costPerUnitA) {
        output.innerHTML = "Drink B is a better deal. Choose Drink B!";
      } else {
        output.innerHTML = "Both drinks offer the same value.";
      }
    }
  </script>

</body>
</html>

OUTPUT:
Which Drink is a Better Deal?
Drink A: Rs.25 for size 11

Drink B: Rs.23 for size 9

Check Deal
Drink A is a better deal. Choose Drink A!
