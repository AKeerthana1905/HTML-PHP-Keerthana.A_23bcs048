47. Delete an element from the below array. And print the array elements in PHP. $x = array (1, 2, 3, 4, 5);
CODE:
<?php
$x = array(1, 2, 3, 4, 5);

// Delete element at index 2 (value 3)
unset($x[2]);

// Reindex the array so indexes are sequential
$x = array_values($x);

// Print array elements
foreach ($x as $value) {
    echo $value . " ";
}
?>

OUTPUT:
1 2 4 5
