83. Develop a PHP script to accept email addresses and validate it. Display the domain name of the
email and result of validation.

<?php
if ($_SERVER["REQUEST_METHOD"] == "POST") {
    $email = $_POST["email"];

    // Validate email
    if (filter_var($email, FILTER_VALIDATE_EMAIL)) {
        echo "✅ Valid Email Address: " . htmlspecialchars($email) . "<br>";

        // Extract domain
        $domain = substr(strrchr($email, "@"), 1);
        echo "📧 Domain Name: " . $domain;
    } else {
        echo "❌ Invalid Email Address!";
    }
}
?>

<!DOCTYPE html>
<html>
<head>
    <title>Email Validation</title>
</head>
<body>
    <h2>Email Validation Form</h2>
    <form method="post">
        Enter Email: <input type="text" name="email" required>
        <input type="submit" value="Validate">
    </form>
</body>
</html>
